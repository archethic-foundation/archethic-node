defmodule ArchEthic.Metrics.Poller_test do
  use ExUnit.Case, async: true
  alias ArchEthic.Metrics.Helpers

  # describe "initialization" do
  #       test "accepts a measurement count on start" do
  #             assert {:ok, _pid} = start_supervised( start_supervised!(ArchEthic.Metrics.Poller)
  #       end
  # end
end
